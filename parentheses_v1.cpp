#include<iostream>
#include<stdio.h>
#include<string.h>
using namespace std;

char ary[1000000];

int main(){
    int i,j,k;
    long long preS = 0;
    scanf("%s", ary);
    int len = strlen(ary);
    for(i=0;i<len;i++){
        switch(ary[i]){
            case '?':
                break;
            case '(':
                break;
            case '(':
                break;
        }
    }
    return 0;
}